# GGparrot 디자인 시스템

껄무새(GGparrot)의 색·조판·컴포넌트 규격. 세 개의 공개 디자인 명세를 역할별로 나눠 조합했고,
벗어난 지점은 전부 이유와 함께 [§10](#10-명세를-벗어난-지점)에 적어둔다.

| 출처 | 가져온 것 |
|---|---|
| **binance** | 다크 표면(3단) · 단일 액센트 노랑 · 다크 등락 색 · 숫자와 본문 서체 분리 |
| **toss** | 컴포넌트 규격 · 조판 램프 · 간격 · 라운드 사다리 · 모션 · 카피 어조 · 라이트 중성색 |
| **spacex** | 카드·컨테이너를 쓰지 않는 구성 |

명세 원본: `~/.claude/skills/{binance,toss,kraken,...}/DESIGN.md`
시각 레퍼런스(확정본 미리보기):
- 위계 + 라이트 모드 — https://claude.ai/code/artifact/bdde928b-72bd-4d64-a675-11aaff78f049
- 텍스트 사다리(작을수록 밝게) — https://claude.ai/code/artifact/9dfd18a0-b6fa-47fa-b9a9-81086549b8d3

---

## 1. 원칙

1. **작을수록 대비를 높인다.** 다크에서는 작은 글씨일수록 밝게, 라이트에서는 어둡게 — 같은
   규칙의 양면이다. 어두운 바닥에서 밝은 가는 획이 번져 보이는 현상(halation) 때문에,
   계산상 AA를 통과하는 대비도 크기가 작으면 실제로는 흐리게 읽힌다. 수치가 아니라 이 규칙이 기준이다.
2. **제목과 본문은 네 축을 동시에 가른다** — 크기·무게·자간·행간. 한 축만 바꾸면 위계가 안 선다. ([§4](#4-위계--제목과-본문))
3. **영역을 감싸는 큰 상자는 쓰지 않는다.** 표·목록·본문·스탯은 캔버스 위에 괘선으로만 구분한다.
   반면 **손으로 누르는 작은 것(버튼·뱃지·칩)과 알림·경고·폼·모달은 상자를 유지한다** — 눌리는
   것처럼 보여야 하고, 끼어드는 것은 끼어든 것처럼 보여야 한다.
4. **노란 채움은 화면당 하나** — 가장 중요한 행동에만. 나머지 강조는 명도와 무게로. ([§5 노랑 매트릭스](#노랑-사용-매트릭스))
5. **숫자는 전용 서체로.** 본문은 Pretendard, 수치는 JetBrains Mono + `tabular-nums` (`.num`).
6. **유리(backdrop blur)는 무언가가 실제로 무언가를 덮을 때만.** 정지한 카드에는 쓰지 않는다. ([§7](#7-깊이와-유리))
7. **13px 이하에 weight 400을 쓰지 않는다.** 최소 500. 크기를 줄이면 무게와 밝기를 같이 올린다.

---

## 2. 색

### 2.1 다크 (기본)

표면은 binance 3단. 괘선은 카드와 같은 색 — "테두리가 잉크 선이 아니라 표면의 한 단계처럼" 읽힌다.

| 역할 | 값 | #0b0e11 대비 | Tailwind | 쓰는 곳 |
|---|---|---|---|---|
| 캔버스 | `#0b0e11` | — | `bg-slate-50` | 페이지 바닥 |
| 면 | `#1e2329` | — | `bg-slate-100` | 작은 버튼·입력·아바타 배경 |
| 한 단계 위 | `#2b3139` | — | `bg-slate-200` | 눌림·호버 면 |
| 괘선 | `#2b3139` | — | `border-slate-200` | 행 구분·구획 |
| 강한 테두리 | `#3a424e` | — | `border-slate-300` | 보조 버튼 외곽 |

**텍스트 사다리** — 단계는 밝기 순이 아니라 **역할** 순이다. 그래서 작은 글씨용(12.3:1)이
본문용(10.3:1)보다 밝다. 직관에 어긋나지만 어두운 화면에서는 이게 맞다.

| 역할 | 값 | 대비 | 크기·무게 | Tailwind | 쓰는 곳 |
|---|---|---|---|---|---|
| **강조** | `#eaecef` | 16.4:1 | 15px+ / 600–700 | `text-slate-900` | 제목 · 이름 · 수치 |
| **본문** | `#b7bec9` | 10.3:1 | 17px / 400 | `text-slate-600` | 문단 · 설명문 |
| **작은 글씨** | `#c9cfd8` | 12.3:1 | 13–15px / 500–600 | `text-slate-700`, `text-slate-500` | 보조 설명 · 라벨 · 단위 · 캡션 — **읽어야 하는 모든 작은 것** |
| **장식** | `#9aa4b2` | 7.7:1 | — | `text-slate-400` | 화살표 · 구분자 · chevron — **읽을 필요 없는 것만** |
| 퇴출 | ~~`#929aa5`~~ | 6.8:1 | — | — | AA는 통과하지만 실제로 안 읽힘. 안 씀 |
| 퇴출 | ~~`#707a8a`~~ | 4.1:1 | — | — | binance `muted` 원값. AA 미달 |

**읽어야 하는 글자의 하한은 12.3:1(`#c9cfd8`)이다.** 7.7:1(`#9aa4b2`)조차 장식 전용.

| 시맨틱 | 값 | 규칙 |
|---|---|---|
| 상승 | `#0ecb81` (`text-green-600`) | **글자 색으로만.** 채움·카드 배경 금지 |
| 하락 | `#f6465d` (`text-red-600`) | 동일 |

### 2.2 라이트

토스 중성색(OKLCH→hex 변환). 순수 회색이 아니라 **차가운 navy 기운**이 돌아 흰 화면이 누렇게
뜨지 않는다 — 노란 액센트와 같이 쓸 때 특히 중요하다. 다크와 **완전 대칭**: 작을수록 어둡게.

| 역할 | 값 | 토스 토큰 | 흰 배경 대비 | Tailwind |
|---|---|---|---|---|
| 캔버스 | `#ffffff` | white | — | `bg-surface` |
| 페이지 바닥 | `#f6f8fa` | grey-50 | — | `bg-slate-50` |
| 면 | `#eef1f4` | grey-100 | — | `bg-slate-100` |
| 괘선 | `#dee3e7` | grey-200 | — | `border-slate-200` |
| **강조** | `#141f2c` | grey-900 | 16.6:1 | `text-slate-900` |
| **작은 글씨** | `#2d3a48` | grey-800 | 11.6:1 | `text-slate-700` |
| **본문** | `#4b5765` | grey-700 | 7.4:1 | `text-slate-500`/`600` |
| **장식** | `#6a7480` | grey-600 | 4.7:1 | `text-slate-400` |
| 상승 | `#007738` | green-500 | 5.7:1 | `text-green-600` |
| 하락 | `#c81e33` | (조정) | 5.7:1 | `text-red-600` |

토스 `red-500`(`#f03848`)은 흰 배경에서 **3.9:1로 미달**이라 `#c81e33`으로 진하게 조정했다.
**상승·하락의 명도를 5.7:1로 맞춘 것이 의도다** — 한쪽이 더 튀면 손익이 편향돼 보인다.

### 2.3 브랜드 · 포인트

| 역할 | 값 | Tailwind |
|---|---|---|
| 브랜드 채움 | `#fcd535` | `bg-brand` |
| 브랜드 위 글자 | `#181a20` (12.2:1) | `text-brand-ink` — **흰색 금지** |
| 호버 | `#ffe066` | `hover:bg-brand-hover` |
| 포커스 테두리 | 다크 `#fcd535` / 라이트 `#c9a300` | `brand-line` |
| 포인트(🪙) | 기본 인디고 계열, 내 활동의 보유 포인트는 브랜드색 | `bg-indigo-50 text-indigo-800` 등 / 프로필 `--c-brand-line` |

포인트가 인디고인 이유: 원래 앰버(`#fcd34d`)였는데 브랜드 노랑(`#fcd535`)과 사실상 같은 색이라,
노랑이 CTA를 맡으면서 옛 브랜드색(인디고) 자리로 이전했다. 내 활동의 보유 포인트는 2026-09-10 사용자 요청에 따라 브랜드색으로 강조한다(§6 profile).

### 노랑 사용 매트릭스

노랑이 **어디에 어떤 형태로 허용되는지**가 두 테마에서 다르다. 흰 배경 위 노란 글자는 **1.4:1**로
아예 보이지 않기 때문이다.

| 쓰임 | 다크 | 라이트 |
|---|---|---|
| 주요 버튼 채움 | ✅ `bg-brand` + `text-brand-ink` | ✅ 동일 |
| 강조 **글자** (수치·뱃지 텍스트) | ✅ 노랑 글자 가능 | ❌ **금지** — 명도·무게로 대체 |
| 1위 표시 | 순위 숫자를 강조색으로 반전 | **아바타를 노랑으로 채움** |
| 뱃지 | 노랑 18% 배경 + 노랑 글자 | 옅은 노랑 배경 `#fdf3c9` + **갈색 글자 `#6b5205`** |
| 입력 포커스 테두리 | `#fcd535` | `#c9a300` (원색은 흰 배경에서 테두리로도 안 보임) |
| 왼쪽 규칙(배너) | ✅ `border-l-2 border-brand` | ✅ 동일 |
| 큰 면적 채움·본문 글자 | ❌ | ❌ |

---

## 3. 조판

### 3.1 서체

| 용도 | 서체 | 근거 |
|---|---|---|
| 본문 · UI | **Pretendard Variable** (wght 45–930) | 토스가 자체 서체(TPS) 대체로 공식 권장 — 메트릭·한글 자형·tabular figure가 거의 1:1. 한글 자소가 라틴 대비 크게 설계돼 같은 px에서 시스템 폰트보다 커 보인다 |
| 숫자 · 수치 | **JetBrains Mono Variable** | binance가 BinancePlex 대체로 지정. OFL 라이선스. 슬래시 0과 각진 획이 "계기판" 인상을 만든다 — `tabular-nums`만으로는 안 나오는 질감 |

가변 폰트라 **500·560·600 같은 중간 무게**를 정확히 쓸 수 있다 — "크기가 줄면 무게를 한 단계만
올린다" 같은 미세 조정이 이 시스템의 전제다.

**적재** — `frontend/src/assets/fonts/`. 앱이 실제로 쓰는 한글 695자 + 라틴을
`Pretendard-0.woff2`(157KB)에 담아 항상 로드하고, 나머지 한글 완성형 전체는 8조각으로 나눠
`unicode-range`로 필요할 때만 받는다. 0번을 CSS에서 **마지막에 선언**해 겹치는 글자에서
우선하게 한다. 원본 TTF(6.5MB)는 서브셋 재생성용으로 저장소에 보관한다.

### 3.2 램프 (토스 기반 · 다크 보정)

| 토큰 | 크기 / 행간 / 자간 / 무게 | 쓰는 곳 |
|---|---|---|
| `display` | 36→52 / 1.1 / −0.035em / 700 | 전면형 시작 가이드의 장면 제목 (`.t-display`) |
| `metric` | 36→48 / 1.05 / −0.03em / 700 | 결과 화면의 단일 핵심 수치 (`.t-metric.num`) |
| `h2` | 24 / 1.3 / −0.02em / 700 | 화면 제목 (`text-2xl font-bold`) |
| `h4` | 20 / 1.35 / −0.015em / 700 | 섹션·패널 제목 (`text-xl font-bold`) |
| `title` | 17 / 1.3 / −0.015em / 700 | **행 제목 · 카드 제목** (`text-[17px] font-bold tracking-tight`) |
| `body` | 17 / 1.55 / −0.005em / 400 | 문단 (body 전역 기본) |
| `label` | 15 / 1.25 / −0.005em / 600 | 표 라벨 · M 버튼 |
| `small` | 14 / 1.5 / −0.005em / **500** | 보조 설명 · 도움말 (`text-sm font-medium`) |
| `caption` | 13 / 1.4 / 0 / **600** | 단위 · 캡션 · 스탯 라벨 (`text-[13px] font-semibold`) |

토스 원본은 `body-3` 13/400, `caption` 12/500이지만 **다크에서 안 읽혀 한 단계씩 올렸다**
(크기 +1, 무게 +100). 비율 관계는 유지했으므로 위계는 그대로다.

제목 자간은 **조이고**(−0.015~−0.02em: 덩어리로 읽힘), 본문 자간은 **푼다**(−0.005em: 한 글자씩
읽힘). 전역 CSS가 `h1–h4`에 −0.02em을 걸고, `.text-xs`에 weight 500 하한을 강제한다.

### 3.3 램프는 클래스로만 부른다

위 표의 아홉 단계는 **`.t-display .t-metric .t-h2 .t-h4 .t-title .t-body .t-label .t-small .t-caption`** 으로 구현돼 있다.
자리마다 `text-[13px] font-semibold` 처럼 손으로 적으면 반드시 흩어진다 — 실제로 265곳이 17가지
조합으로 갈라져 있었고, 같은 13px이 어디선 500, 어디선 600이었다. **크기·행간·자간·무게는 한
덩어리(§4)이므로 낱개로 쓰지 않는다.** 한 단계만 무게를 올릴 때만 `t-small font-semibold` 처럼 겹친다.

색은 램프에 넣지 않았다 — 역할별로 따로 고르는 축이라(§2 텍스트 사다리) 묶으면 잘못된 짝이 굳는다.

### 3.4 한글 줄바꿈

```css
word-break: keep-all;      /* 어절(띄어쓰기) 단위로 끊는다 */
overflow-wrap: break-word; /* 끊을 자리 없는 긴 토큰만 예외로 강제 분해 */
```

기본값(`normal`)은 한글을 **음절 아무 데서나** 자른다. 실제로 "매수 추천이 아니에요"가
`아` / `니에요` 로 잘려 있었다. 한글 화면에서는 거의 항상 `keep-all` 이 맞다. 단 이것만 걸면
URL·해시·에러 문자열이 넘치므로 `break-word` 를 짝으로 건다.

제목은 `text-wrap: balance`(줄 길이 고르게), 문단은 `text-wrap: pretty`(마지막 줄 고아 단어 방지).

---

## 4. 위계 — 제목과 본문

기준 쌍은 board-row의 이름 ↔ 매크로 설명이다. **네 축이 전부 갈린다:**

| 축 | 제목 (`title`) | 본문 (`small`) | 차이 |
|---|---|---|---|
| 크기 | 17px | 14px | **3px** |
| 무게 | 700 | 500 | **200** |
| 자간 | −0.015em | −0.005em | 조임 ↔ 풀림 |
| 행간 | 1.3 | 1.5 | 밀착 ↔ 여유 |

1px + 무게 100 차이(예: 15/600 ↔ 14/500)는 위계를 만들지 못한다 — 실패 사례로 기록해 둔다.

### 자리별 적용표

확정 과정에서 검수한 11개 자리. 새 화면을 만들 때 이 표에 맞춘다.

| 자리 | 규격 | 색 역할 |
|---|---|---|
| 행 제목 (리더보드 이름 등) | 17 / 700 / tracking-tight | 강조 |
| 매크로 설명 · 행 부제 | 14 / 500 | **작은 글씨** |
| 입력 라벨 | 14 / 600 | 작은 글씨 |
| 입력 도움말 | 14 / 500 | 작은 글씨 |
| 입력 단위 (원 · %) | 15 / 600 | 작은 글씨 |
| 표 라벨 (table-row 좌측) | 15 / 500 | 작은 글씨 |
| 차트 캡션 | 13 / 600 | 작은 글씨 |
| 단계 라벨 (stepper) | 14 / 600 · 현재만 강조색 | 작은 글씨 |
| 칩 · 세그먼트 | 14–15 / 600 | 작은 글씨 |
| 뱃지 | 12 / 700 | 시맨틱 |
| 더보기 chevron · 구분자 | — | **장식** (유일하게 낮은 대비 허용) |

---

## 5. 형태

**라운드** — 토스 사다리. 버튼은 크기와 짝.

```
4 · 8 · 12 · 14 · 16 · 20 · 24 · 999(full)
버튼   XL 56px→16   L 48px→14   M 40px→12   S 32px→10
칩     full pill    아바타 full   입력 12     카드성 표면 16~20
```

**간격** — 4의 배수. 목록 행 간 16(행 패딩 상하 16), 라벨↔입력 8.

**화면 바깥 여백(게터)은 토큰 하나로만 관리한다** — `--site-gutter` (데스크톱 22 / ≤1099px 16).
상단바·본문·푸터가 모두 이 값을 쓰므로 셋의 왼쪽 끝이 한 세로선에서 시작한다.

사이드바가 없던 시절에는 `max-w-6xl mx-auto` 로 뷰포트 중앙에 맞추는 게 맞았다. 지금은 그러면
**사이드바를 뺀 '남은 영역' 안에서 한 번 더 중앙정렬**돼서 헤더 로고와 본문 제목의 왼쪽 끝이
어긋난다. 새 화면의 본문 컨테이너는 `.site-main` 을 쓰고, **폭 제한은 게터가 아니라 안쪽에서**
(`.measure` 또는 `max-w-*`, `mx-auto` 없이) 건다 — 중앙정렬을 겹치면 왼쪽 끝이 다시 흔들린다.

전면 레이아웃(랜딩·실행기)과 로그인 셸의 중앙정렬 폼은 이 규칙의 예외다.

**터치** — 누르는 것은 전부 44×44px 이상 (시각 높이가 작아도 패딩으로 확보).

---

## 6. 컴포넌트

토스 규격 + 우리 색. 상태값까지 포함해 적는다.

### board-row (리더보드 행)
- 구조: `순위(32px) | 종목 로고(36px) | 매크로(이름·배지·등록 캡션) | 전략 설명 | 우측 tabular 수익률 | 반응(아이콘)` — 전체 폭 6열 grid, 열 머리글 있음. ≤1099px 에선 전략이 이름 아래로, ≤639px 에선 반응이 자기 줄로. 1·2·3위 숫자는 금·은·동 + '방어전' 배지. 안내문·목록 끝 문구는 두지 않는다(초기화 시각은 카운트다운이 말한다).
- 캔버스 위 괘선 리스트 — 행 카드 없음. 구분은 `border-b`(괘선) 하나.
- 순위 숫자: `.num` 본문색. **1위만 강조색으로 반전.**
- 아바타: 이름 첫 글자, weight 700, 면 배경. **1위만 `bg-brand text-brand-ink`.**
  이모지 아바타 금지 — 36px 원 안에서 흐릿하고, 토스의 인라인 이모지 금지 규칙과도 충돌.
- 수익률: `.num` 15–16px / 700, 등락색, `+`/`−` 부호 필수.

### board-list (게시판 목록 · 2026-09-09)
- **한국 커뮤니티 게시판의 문법**을 따른다(디시·에펨·클리앙·루리웹·뽐뿌·코인판·더쿠·인벤 벤치마크 — 위키
  `concepts/한국-게시판-목록-디자인-관례`). 카드·아바타·요약 없음, 행 42px, 괘선 1px, 화면당 10행.
- 열(2026-09-10, 퀘이사존·그누보드 참고): `추천수(48, 왼쪽 .num — 0 은 흐림, 1 이상은 brand-line 700) | 제목 15/500 + 제목 뒤 [댓글수](brand-line .num 13/700) + 사진 아이콘 | 글쓴이 13/500 (128) | 시각 (72, 오른쪽) | 조회 (56, 오른쪽)`. 번호 열은 없다.
- 머리(2026-09-10): 제목 옆에 `n건`(15/500 흐림, 그누보드식). 제목 아래 왼쪽 **검색줄** `[제목+내용 ▾][검색어][검색]`(`field-sm` 116 + 240, 2차 버튼; 검색 중이면 `지우기`), 오른쪽 **정렬 segmented(최신순·추천순·조회순·댓글순, 32px) + 글쓰기**. 필터 칩·조작 줄은 두지 않는다. 상태는 주소(`?page&sort&q&field`)에, 기본값은 뺀다. ≤639px 은 검색줄이 `범위 | 검색어 | 검색` 한 줄 격자.
  열 머리글은 리더보드와 같은 12px 흐림 글자 + 아래 `slate-300` 선. 행 전체가 링크(`.board-row`): hover `slate-100`, active `slate-200`, focus `brand-line` 링. 상태면은 `::before` 로 글자 밖 8px 까지 넓게 깐다(번호가 면 가장자리에 붙어 잘린 듯 보이지 않게, 괘선·글자 자리는 그대로).
- 제목은 **보통 굵기** — 굵기는 위계가 아니라 글의 무게. 읽은 글은 `:visited` 로 한 단계 흐려진다. hover 에 1px 밑줄.
- 시각은 `lib/boardText.boardTime` — 7일 안은 `방금 전·N분 전·N시간 전·N일 전`(한 단계 진하게) · 올해 `MM.DD` · 그 전 `YY.MM.DD`(KST). 툴팁·`datetime` 은 전체 시각. 댓글도 같은 규칙.
- ≤639px 은 두 줄(제목 [n] / 글쓴이 · 시각), 번호·머리글은 접는다.
- 페이지 머리는 공용 `PageHeader` — 제목 + meta(`글 n개 · p/q 쪽`) + 우측 `글쓰기`. 쪽 이동은 표 아래 가운데 segmented(현재 쪽만 면). 고지문은 아래 왼쪽 12px.
- 글 상세: 제목 20~24/700 → **괘선 띠**(`글쓴이(사진은 눌러 크게 — 어두운 스크림 위 360px 원) | N분 전 · 조회 n | 편집·삭제`, 위 slate-300 · 아래 slate-200) → 본문 15/1.8(글줄만 900px, 괘선·띠·댓글·댓글창은 목록과 같은 **전체 폭**) → 댓글(글쓴이 13/700 · 시각 .num 12 / 본문 14/1.65 / 괘선) → 본문 아래 가운데 **반응 알약**(`추천 n` · `비추천 n`, 40px, 리더보드 좋아요 문법; 내 표는 brand-line/하락색 테두리, 로그인 없으면 로그인으로) → 댓글 → 아래 `목록`. 본문 구역(`.board-post-main`)은 최소 56vh — 짧은 글에서 댓글창·목록이 바로 올라붙지 않는다. 글 삭제는 `ConfirmDialog`.
- 댓글창(2026-09-10): **로그인 회원만** — 입력칸(2줄, 라벨 없이 자리표시자 `댓글을 남겨보세요`) + 오른쪽 아래 `댓글 등록`(M). 작성자 사진·이름은 여기 두지 않는다(등록된 행이 보여 준다). 로그인 전에는 `댓글은 로그인한 회원만 남길 수 있어요` + 로그인(S 2차) 한 줄. 댓글 행은 `사진 24(가운데 맞춤) · 닉네임 13/700 · N분 전`, 지우기는 쓴 사람만(옛 익명 댓글은 글쓴이) — ConfirmDialog. 추천·비추천도 로그인 전엔 눌리지 않고 아래 안내 한 줄.
- **폭(2026-09-10)**: 목록·글·글쓰기 모두 가운데 1160px 컬럼(`.board-page`·`.board-post-page`) — §5 의 '왼쪽 정렬·폭 제한 없음' 예외. 전체 폭으로 퍼지면 표와 글이 너무 넓다(퀘이사존 1200 컨테이너).
- 글·글쓰기·수정 화면에 `목록`·`뒤로` 버튼을 두지 않는다 — 글 아래 목록과 취소가 그 역할이다.
- 글쓰기(2026-09-10): 목록 위에 끼워 넣지 않고 **전용 페이지 `/board/write`** 로 이동한다(`pages/BoardWrite.jsx`). 골격은 글 보기와 같다 — `← 목록` → 제목 `새 글 쓰기`(글 제목 규격) → 전체 폭 폼: 제목(40) → 내용(200px+) → 첨부 미리보기(112px 정사각 썸네일, 번호·지우기, 붙인 순서) → 아래 줄은 오른쪽 `취소 · 등록` 만. 사진 규격 안내(`n/10장 · JPG·PNG · 한 장에 2MB · 10장까지`)는 도구 줄 오른쪽 끝 12px, 아래 괘선. **본문은 서식 편집기**(TipTap, `components/BoardBodyEditor`) — 도구 줄(실행 취소·다시 실행 | 굵게·기울임·밑줄·취소선 | 글자 크기 5단·글자색 6색 | 큰·작은 제목 | 왼쪽·가운데·오른쪽 정렬 | 글머리·번호 목록·인용·링크 | 사진·서식 지우기)은 32px 고스트 버튼, 켜진 상태는 `slate-200` 면. 편집면은 textarea 와 같은 `.field`. **사진은 커서 자리에 들어가고, 끌어서 옮기고, 골라서 모서리 손잡이(노랑)로 폭을 바꾸며, 정렬 버튼으로 왼쪽·가운데·오른쪽에 둔다**(붙여넣기·끌어다 놓기도). 저장은 HTML(`body_format=html`): 새 사진은 `data-key="new:N"` 자리로 보내고 서버가 저장 뒤 주소를 붙인다. 서버가 nh3 로 허용 목록(문단·제목 h2·h3·굵게·기울임·밑줄·취소선·목록·인용·링크·사진·span 의 color/font-size·text-align·width) 밖을 전부 버리고, 사진 주소는 우리 서버 것만 남긴다. 글 보기는 같은 규칙(`.board-rich`)으로 그리되 DOMPurify 로 한 번 더 거른다. 본문이 가리키는 사진이 곧 남는 사진(빠진 사진은 지움). 옛 글(글자 + `[사진n]`)은 서버가 같은 HTML 로 바꿔 준다. 목록 발췌는 서식을 뺀 글자만 쓴다.
- 글 수정(2026-09-10): 본인 글의 괘선 띠 오른쪽에 `편집 · 삭제`(S 고스트 버튼, 삭제는 hover 하락색). 편집은 `/board/:id/edit` — 같은 폼에 제목·본문·올린 사진이 채워져 있고, 사진을 빼거나 더 붙일 수 있다. 저장·취소 모두 그 글로 돌아간다. 남의 글 주소로 들어오면 글 보기로 보낸다. 입력·라벨 규격은 댓글창과 같다. 등록하면 새 글로, 취소하면 목록으로. 로그인 전이면 `/login?next=/board/write`.
- 스타일은 `pages/Board.css` 한 파일(상단바 공유 스타일시트와 분리).

### profile (내 활동·설정 전면 재설계 · 2026-09-10)
- 사용자가 제공한 프로필 예시를 기준으로 **회원 영역과 작업 공간을 분리**한다. 이 페이지는 §1-3의 큰 면 금지 예외: 회원 영역, 포인트 요약, 실제 매크로 선택 카드에만 표면과 테두리를 사용한다. 공통 색·서체·내비게이션은 유지한다.
- 데스크톱: 최대1320px, 왼쪽240px 회원 영역 + 오른쪽 포인트 요약/활동. 사진144px, 이름30px, 현재 등급·소개·가입일. 우상단24px 톱니바퀴는44px 터치 영역. 1100px 미만에서는 한 열이며 작은 모바일에서 사진88px, 이름26px.
- 모바일 회원 영역은 사진 가운데, 이름·등급 한 줄, 아래 가입일로 묶는다. 긴 이름은 줄바꿈한다. 별도의 페이지 타이틀이나 홍보 문구를 붙이지 않는다.
- **편집:** 중복된 `프로필 편집` 링크는 두지 않는다. 톱니바퀴는 `/mypage/settings?tab=security`로 설정을 열며 프로필 탭에서 편집한다. 사진은 `/mypage/settings`로 바로 이동한다. 메인 화면에 펼쳐지는 계정 폼을 두지 않는다.
- **회원 기능:** 프로필의 가입일 아래 로그아웃 버튼을 둔다. 회원 키는 설정 → 계정 및 보안에서 관리 버튼으로 펼칠 때만 조회하고 기본 마스킹·보기·복사·재발급을 제공한다. 계정·설정 탭 전환 시 닫으며, 로그아웃은 인증을 지우고 로그인 화면으로 이동한다.
- **간격·정렬:** 박스 안쪽은 기본24px, 모바일·좁은 데스크톱16px. 주요 영역 간격은24px(모바일16px), 정보 묶음16px, 라벨과 값8px, 종목과 보조 정보4px. 나란한 매크로 카드는 제목·전략·수치·버튼 행의 높이를 공유한다. 금액·구매일은 같은20px 행 높이. 거래·게시글 목록은 첫 행부터 상하16px, 거래 제목과 증감은 첫 행에 맞춘다.
- **포인트 요약:** 보유 포인트·판매 수익·누적 판매를 같은 크기·기준선의 세 열로 놓는다. 데스크톱26px, 모바일20px(359px 이하18px) `.num`. 7자를 넘는 값은 세 지표 모두 라벨 왼쪽·값 오른쪽의 세로 목록으로 바꾼다. 보유 포인트는 브랜드색(`--c-brand-line`). 요약을 누르면 대응 내역을 연다.
- **분류:** `매크로 / 포인트·판매 / 게시글` 세 개가 모든 너비에서 한 줄이다. 매크로의 하위 필터는 `만든 매크로 / 구매한 매크로`, 포인트·판매는 `판매 내역 / 포인트 내역`이다. 하위 필터에 개수를 표시하고 방향키·Home·End를 지원한다. `?tab=created|purchased|sales|ledger|posts`를 유지한다.
- **매크로 카드:** 넓은 화면 두 열, 599px 이하 한 열. 코인36px·종목 + 전략 전문 → 같은 기준선의 판매/구매 수치 → 오른쪽 빌더 행동. 만든 매크로에는 판매 건수·누적 수익, 구매한 매크로에는 구매 금액·구매일을 표시한다. 없는 가격이나 썸네일을 만들어 넣지 않는다. 사용할 수 없는 데이터는 `불러오기 불가`. 모바일 버튼은36px 테두리와44px 누름 영역을 구분한다.
- 모바일은 중복 제목을 숨기고 두 매크로 필터 옆44px + 버튼으로 생성한다(접근 가능한 이름 `매크로 만들기`). 빈 목록에도 생성 진입은 유지하며 중복 CTA는 두지 않는다.
- 판매·원장은 내용/회원/날짜 왼쪽, 증감/잔액 오른쪽. 판매가 원장에도 기록되므로 두 내역의 수를 합치지 않는다. 게시글은 제목·댓글·첨부·날짜와 글쓰기.
- **등급:** 현재 단계만 SVG와 짧은 이름으로 표시. 클릭하면 다섯 단계와 조건을 대화상자에서 제공한다. Phosphor duotone, 단계는 API `tier.ladder`.
- **설정:** `ProfileSettings.jsx/.css`. 데스크톱200px 왼쪽 탐색 + 최대640px 폼, 모바일은 위쪽 `프로필 / 계정 및 보안` 탐색. 프로필 폼은96px 사진 변경·삭제, 프로필명·소개, 하단 취소·저장. 저장 후 페이지에 남고 취소는 프로필로 복귀한다. 모바일 자동 입력 포커스 없음.
- 계정 및 보안에는 이메일·가입 방식·비밀번호 변경(가능한 회원)·로그아웃·탈퇴. 비밀번호/탈퇴 대화상자는 인증·계정별 요청 보호를 유지한다. 미완성 계정 캐시는 최신 정보 수신 전까지 편집 폼을 열지 않는다.
- 공통 `UserAvatar`는 회원 ID로 헤더·리더보드 채팅·게시판에 연결한다. 미등록·실패는 이름 첫 글자, 익명 글쓴이에게 회원 사진을 연결하지 않는다.
- 구현과 검증 근거: [프로필·설정 재설계](docs/design/profile-redesign.md).

### table-row (지표 표)
- `라벨 | 값 (| 증감)` 열, 행 사이 1px 괘선, 마지막 행 괘선 없음.
- 라벨 15/500 작은 글씨, 값 17/700 `.num` 강조색, 등락은 시맨틱 색.
- 스탯을 회색 상자 그리드로 만들지 않는다 — 이 행 구조가 대체다.

### stat (상자 없는 수치)
- 캡션(13/600 작은 글씨) 위, 수치(`.num` 700) 아래. 배경·테두리 없음. 여백으로만 구분.

### bar-chart
- 막대는 면 색(`#2b3139` / `#dee3e7`) 기본, **강조 막대 하나만 노랑**. 상단만 6px 라운드.
- **축선·격자·구분선을 그리지 않는다.** 캡션은 좌우 끝에 13/600.
- equity curve 등 선형 차트도 동일: 격자 최소화, 좌표축 잉크를 장식 단계 이하로.

### carousel (시장·규제 한눈에 — `.news-carousel`)
- 기사마다 **사진(og:image) 위에 제목** 한 장. 사진은 서버가 Google News 링크의 원문을 복원해 배경에서 채운다
  (`backend/app/news_images.py`, `image_status: pending` 이면 웹이 몇 번 더 조용히 받아 온다). 사진이 없으면
  고정 어두운 면(#2b3139→#0b0e11, 테마 토큰은 다크에서 반전되므로 쓰지 않는다) + 출처 이름을 크게 흐리게.
- **가운데 카드만 선명**, 양옆(±1·±2)은 `--offset` 에 따라 작아지고(12%씩) 흐려지며(3px씩) 어두워진다 — 유리 너머.
  카드 폭 72%(최대 520), 높이 240~320, radius 16, 흰 22% 선. 스크림은 아래로 갈수록 진한 `#0b0e11`.
- **자동으로 넘어가지 않는다.** 화살표(40px 유리 원형, 좌우 10px)·←→ 키·스와이프(40px)·옆 카드 클릭으로만.
  가운데 카드는 원문 링크(`원문 열기` 유리 알약), 옆 카드는 "가운데로 가져오기" 버튼.
- 아래 줄: 점(선택 18px 노랑 — 화면의 노랑 하나) + `n / 총` 카운터.
- 전환 460ms, `prefers-reduced-motion` 이면 즉시.

### treemap (경주마 동향 — `.news-racer-map`)
- 한 직사각형을 스퀘어리파이드 알고리즘으로 나눈 벤토(Finviz 맵의 **배치만** 빌린다). **면적 ∝ √상승률** —
  상승률 그대로면 +115% 하나가 절반을 먹고 +4% 는 글자를 못 얹는다(`lib/treemap.js`).
- 타일은 색면 없이 **유리**(§7 상단바와 같은 `glass-bg`/`glass-alpha` + blur 20) + 1px `slate-300` 선, radius 14,
  타일 사이 8px. 상승률만 `green-600`/`red-600` 글자. 순위는 `brand-line`.
- 타일은 **기사 한 건**만 품는다(제목 + 출처·시간·n/총). 모든 타일이 같은 tick 으로 다음 기사로 넘어가고,
  `key` 교체로 진입 애니메이션(420ms, 아래서 떠오름)이 **동시에** 돈다. 탭이 숨겨지면 멈춘다.
- **크기 맞춤은 JS 가 픽셀로 계산한다**(`tileFit`): 면적의 제곱근으로 글자 배율(0.8~2), 순위 숫자는 로고와 같은 높이
  (`--tile-icon`), 남는 높이에서 제목 줄 수(2~6)를 역산한다. 제목이 짧고 줄이 남으면 커뮤니티 글의 본문 요약을 채운다.
- 지표(현재가 · 거래대금)는 **한 줄, 줄바꿈 없음** — 폭이 260px 미만이면 거래대금을 뺀다.
- 안폭 130px 미만은 순위·로고를 한 줄로 두고 그 아래 티커·상승률(`is-stacked`), 64×40 미만은 티커만(`is-tiny`).
- 티커는 빌더 링크, 기사는 원문 링크. 선택 상태 없음.

### 차트 공통 규칙

| 규칙 | 이유 |
|---|---|
| **발산 스케일은 두 색 + 무채색 가운데.** 무지개 금지, 가운데에 색상 금지 | 가운데는 "아무것도 아님"으로 읽혀야 한다. 히트맵이 red→**yellow**→green 이었는데, 노랑이 브랜드 강조색이라 본전 칸이 '좋음'으로 읽혔다 |
| **발산의 기준점은 데이터 중앙이 아니라 의미의 0** | 전 구간이 이익인 격자에서 '가장 덜 번 칸'이 빨갛게 칠해지고 있었다 |
| **선·면 차트에는 호버(크로스헤어+값)를 기본 탑재** | 단, 툴팁이 값을 읽는 **유일한** 통로면 안 된다 — 시작·최종값은 캡션에 직접 적는다 |
| **끝점을 강조**(표면색 링 두른 점) | 곡선이 어디서 끝났는지가 결론이다 |
| **격자선은 실선 헤어라인.** 파선은 임계선에만 | 파선 격자는 '추정·임계'로 잘못 읽힌다 |
| **SVG의 viewBox = 실제 그려질 크기** | 340×172로 그려 880px에 늘리면 배율 2.6배 — 라벨 10.5px이 27px로 찍혀 페이지 제목보다 커진다 |
| SVG 안 링·강조는 `#fff` 하드코딩 금지, `--c-slate-50` 등 토큰 | 다크에서 흰 테두리만 튄다 |

**`.num`(고정폭)은 큰 단독 수치에도 유지한다.** 일반적으로 hero 숫자엔 tabular-nums를 피하지만,
우리 수치 서체는 binance 규격을 따른 **모노스페이스(JetBrains Mono)** 라 어차피 자폭이 같다 —
"계기판" 질감이 §1-5의 의도다.

### text-field
- 48px(`h-12`) · radius 12(`rounded-xl`) · 면 배경 · 테두리 투명.
- **포커스: 캔버스 배경으로 반전 + 1.5px `brand-line` 테두리.** (다크 노랑 / 라이트 `#c9a300`)
- 에러: 1.5px 하락색 테두리 + 아래 14/500 하락색 도움말.
- 값이 숫자면 `.num`, 단위는 Pretendard 유지(고정폭 한글·기호는 어색하다).

### button
- 크기 4단(§5), 채움 3종:
  - **primary** `bg-brand text-brand-ink hover:bg-brand-hover` — 화면당 하나
  - **secondary** 면 배경 + 강조색 글자, 호버 시 한 단계 위 면
  - **ghost/text** 투명 + 브랜드색 또는 본문색 글자
- 눌림: 밝기 −18% (`filter`) 또는 검정 26% 오버레이. **그림자·inner shadow 금지.**
- 비활성: 노드 전체 `opacity 0.30`~`0.40` — 부분 회색 처리하지 않는다.

### badge (22px · radius 6 · 12/700)
| 종류 | 다크 | 라이트 |
|---|---|---|
| AI | `rgba(252,213,53,.18)` + 노랑 글자 | `#fdf3c9` + `#6b5205` |
| 위험(레버리지) | `rgba(246,70,93,.18)` + `#ff8f9f` | `#fde8ea` + `#a11827` |

### chip (34px full pill)
- resting: 1px 괘선 테두리 + 투명 배경 + 작은 글씨색.
- active: `bg-brand text-brand-ink` 테두리 제거. (다중 필터용 — 단일 선택은 segmented)

### segmented-control
- 트랙: 면 배경 + radius 12 + 안쪽 4px. 분절 36px / radius 10.
- 선택 분절: 다크 `한 단계 위 면` + 그림자 1px / 라이트 **흰 배경** + `0 1px 3px rgba(1,10,37,.16)`.
- 미선택: 작은 글씨색, 호버 시 강조색.

### progress-stepper
- 26px 원형 노드 + 2px 연결선. 완료: 노랑 채움+체크 / **현재: 노랑 채움 + 4px 글로우 링
  `rgba(252,213,53,.22)`** / 미도달: 면 배경 + 작은 글씨색.
- 라벨 14/600, 현재 단계만 강조색.

### §1-4 예외 — 화면에 노랑이 둘인 자리

리더보드는 '매크로 만들기'(주요 CTA)와 채팅 버튼이 둘 다 브랜드 노랑이다. 채팅 버튼은 본문
흐름 밖에 떠 있는 상시 유틸리티라 CTA 와 같은 층에서 겨루지 않는다고 보고 예외로 둔다
(2026-09-08, 후보 6종 비교 후 결정). 깃털은 바탕과 같은 노랑이라 채움 대비가 없고 제
외곽선(#1e2329)으로 형태가 선다. 열림 링은 노랑이 아니라 `slate-900` 을 쓴다 — 노란 버튼
위의 노란 링은 보이지 않는다.

- **옮기기·투명도(2026-09-10)**: 데스크톱은 버튼을 잡고 6px 넘게 끌면 이동(놓는 순간의 클릭은 열고 닫기가 아니다),
  자리는 `localStorage chat:placement`(오른쪽·아래 여백 px)에 남고 화면·시트가 밖으로 나가지 않게 자른다.
  시트 머리의 닫기 왼쪽에 눈 아이콘 + 슬라이더(25~100%, `chat:opacity`) — 시트 전체 불투명도
  `--chat-sheet-opacity`. 등장 키프레임의 끝 프레임도 이 변수를 써야 인라인 값이 덮이지 않는다.
  모바일은 오른쪽 아래 16px + 안전 영역에 고정하며 드래그를 받지 않는다. 저장한 데스크톱 좌표는 모바일에서 적용하거나 덮어쓰지 않는다.

### dialog
- radius 20 · 안쪽 24px · 면 배경 · `0 12px 32px` 그림자(모달이므로 허용).
- **CTA는 가로가 아니라 세로로 쌓는다**(각 48px, primary 위) — 오누름 방지 + 긴 한글 라벨 줄바꿈 방지.
- 스크림 `rgba(0,0,0,.56)`.

### top-bar (상단바)

어느 화면에나 얹히는 크롬이라 **조용한 유틸리티 한 줄**이어야 한다. 한때 한 줄에 다섯 가지
처리가 섞여 있었다 — 노랑 뱃지 · 맨 텍스트 · 인디고 뱃지 · 맨 텍스트 · 채움 버튼. 게다가
가장 무거운 채움을 **테마 토글**(가장 덜 중요한 컨트롤)이 입고 있었고, 그 회색 상자가
선택된 내비 분절과 똑같이 생겨서 "여기가 현재 페이지"와 "이걸 누르세요"가 구분되지 않았다.

| 자리 | 규격 |
|---|---|
| 로고 | `t-title` |
| 내비 | segmented — **선택된 하나만** 면 배경(`.seg-item-on`) |
| 오른쪽 묶음(2026-09-09 수정) | 실행기 설치 → 사용법 → 테마 → 프로필. 같은 기능군은 같은 크기·표면·간격으로 배치. 꾸밈 구분선·아이콘 타일·반복 설명을 추가하지 않음 |
| 실행기 설치 · 사용법 | 데스크톱은 **두 링크 모두 104×44px 클릭 영역**, 20px 아이콘 + 14px 라벨, 사이 6px. 상태 배경만 104×36px, 6px 라운드. 1099px 이하는 메뉴 안의 48px 행으로 이동하며 아이콘과 이름을 함께 표시 |
| 아이콘 | `@phosphor-icons/react`의 **regular 원본**으로 통일. 텍스트 옆 그림과 독립 기능 그림의 크기를 구분. SVG 경로·fill·stroke를 CSS로 덮어쓰지 않음. 버튼 부피를 줄이려고 아이콘까지 축소·삭제하지 않음 |
| 테마 · 프로필 (2026-09-10 수정) | 44×44px 클릭 영역. 테마는 전환할 쪽의 채운 해·달22px, 프로필은 사진 또는 이름 첫 글자의32px 원형 아바타. 로그인 전에는 사람 글리프18px. 이름·포인트·chevron은 헤더에 추가하지 않는다 |
| 비로그인 프로필 | 클릭하면 **바로 `/login?next=…`로 이동**. 중간 안내·가입 유도 팝업 없음 |
| 로그인 프로필 | 드롭다운 없이 바로 `/mypage`로 이동. 로그아웃은 내 프로필에서, 회원 키는 설정 → 계정 및 보안에서 제공 |
| 상태 | hover·focus-visible·active·현재 경로 표시. 회원 키 조회 중·오류·복사 완료는 계정 및 보안의 해당 항목에서 안내하고 계정 전환 시 열린 키를 초기화 |
| 색이 있는 것 | **시뮬레이션 고지 뱃지 하나뿐** — 포인트도 인디고를 버리고 본문색 |

목적지가 같은 컨트롤과 같은 의미의 문구를 반복하지 않는다. 이름·포인트는 정보이고,
프로필 이동은 헤더 아바타가 맡는다. 회원 키 상세는 계정 및 보안에서 실행기에 연결할 때만 펼친다.

근거: 위키 `concepts/ui-디자인-원칙.md`의 일관성·여백·불필요한 스타일 제거,
`concepts/gui-위젯-10요소.md`의 익숙한 위젯·직접 이동·최소 메뉴 깊이.
공식 자료의 크기·클릭 영역·도상·대비 조사와 이번 규격의 판단 근거는
[헤더 아이콘 조사와 적용](docs/design/header-icons.md)에 기록한다.

### 모바일 화면 (2026-09-10)
- 1099px 이하의 헤더 첫 줄은 메뉴·로고와 오른쪽의 테마·프로필, 둘째 줄은 시장 브리핑이다. 테마는 프로필 바로 왼쪽에 둔다. 설치·사용법만 펼친 메뉴에 모은다. 터치 기기에서는 헤더 호버 툴팁을 표시하지 않는다. 프로필의 로그인 전후 동작은 데스크톱과 같다.
- 메뉴는 짧은 가로 화면에서도 전체 내용을 스크롤할 수 있다. 데스크톱 너비로 바뀌면 닫고 본문의 `inert`와 스크롤 잠금을 해제한다. PWA 상단·하단 안전 영역을 반영한다.
- 인라인 설명 아이콘은 16px SVG로 유지한다. 터치에서는 투명한 버튼 20px와 바깥 6px 클릭 영역을 사용하며, 아이콘 배경을 44px로 늘리지 않는다.
  코인동향 섹션 제목의 설명 버튼은 바깥 12px 영역으로 총 44px의 투명한 터치 영역을 확보한다.
- 홈은 소개와 게시판을 세로로 이어 표시한다. 자동 슬라이드 전환·고정 페이지점·게시판 이동·큰 선글라스 이미지는 모바일에서 제거한다. 게시판 예시는 3개만 보여주고 반복 설명·안쪽 제목·가짜 페이지 번호는 제거한다. 홍보 제목 밑줄은 글자 크기의 0.12em다.
- 767px 이하 또는 1099px 이하 터치 기기의 코인동향은 작은 트리맵 대신 52px 이상 종목 행과 선택한 종목의 뉴스로 구성한다. 가격·등락률은 고정 크기로 읽히며 긴 코인명은 줄바꿈한다. 같은 범위에서 공통 하단 시세 띠를 숨겨 휴대폰 가로 모드에도 적용한다.
  선택한 종목의 뉴스 목록은 처음 세 항목의 실제 높이까지만 표시하고, 네 번째부터 목록 내부에서 세로로 스크롤한다. 종목을 바꾸면 목록 스크롤은 맨 위로 돌아간다.
- 같은 범위의 리더보드는 순위·28px 코인·심볼·수익률을 한 줄에 둔다. 전략 아래 4px 간격으로 작성자·등록 시각·배지를 붙이고 반복 라벨은 표시하지 않는다. 하단 투표·복사·잠금 해제는 박스 없이 18px 아이콘·12px 숫자로 표시한다. 컨트롤 높이는 32px, 투명한 터치 영역은 최소 44px이며 12px 간격으로 서로 겹치지 않게 한다. 마지막 행도 고정 채팅 버튼을 피해 누를 수 있도록 페이지 하단 72px 여백을 둔다.
- 실행기 지원 여부는 화면 폭이 아닌 OS/기기로 판단한다. 모바일·iPad 데스크톱 모드·비 Windows 기기는 PC 주소 복사와 웹에서 가능한 만들기·상태 조회로 안내하며 실행 마법사를 시작하지 않는다. 좁은 Windows 창에서는 다운로드·실행을 유지한다.

### 사용법
- `/guide`는 실행기 설치와 같은 일반 페이지 셸을 사용한다. 상단의 사용법 링크로 이동하며, 홈 모달로 열지 않는다.
- 상단에 보이는 제목·부제를 두지 않는다. 검색창을 페이지 진입부 중앙에 배치한다. 검색창은 최대 520px·높이 48px, 입력 글자는 16px, 검색 아이콘은 22px. 모바일도 높이는 같고 폭은 화면에 맞춘다. 문서 구조를 위한 `h1`은 화면에서 숨긴다.
- `ggparrot-nav-builder.svg` 원본을 검색창 오른쪽 위에 배치한다(112px, 모바일 96px). 오른쪽 여백은 36px, 모바일 32px로 두어 가장자리에서 조금 안쪽에 놓는다. SVG 내부 여백을 고려해 검색창과 붙이고, 입력 영역을 가리거나 검색창 자체를 키우지 않는다. 온보딩의 임베디드 문서에는 반복하지 않는다.
- 검색 아래에 목차와 본문을 배치한다. 모바일 목차는 접어서 본문을 먼저 볼 수 있게 한다.
- 검색어와 문서 선택은 `q`·`section` URL에 반영한다. 기존 `/?help=…` 주소는 해당 문서 페이지로 연결한다. 온보딩 안의 문서 미리보기는 별도의 검색 상태를 사용한다.

### bottom-cta
- 56px 고정, 위쪽 28px `캔버스→투명` 보호 그라디언트, safe-area inset 존중.
- **화면 안 primary 버튼과 동시에 두지 않는다** (토스 규칙 — 액션 위계 붕괴).

### notice (배너) vs alert (끼어드는 경고)

§1-3은 "알림·경고는 상자를 유지한다"고 하고 이 절은 "배너는 상자 금지"라고 한다. 모순이 아니라
**두 종류**다. 갈림길은 *끼어들었는가*:

| | 쓰는 곳 | 형태 |
|---|---|---|
| **notice** | 화면 흐름 안의 안내·팁·상태 설명 (챌린지 배너, 세션 고정 안내, 가이드 Note/Tip, 과최적화 주의) | 상자 금지. **왼쪽 규칙 2px + 들여쓰기.** `.notice`/`-risk`/`-warn`/`-good` |
| **alert** | 사용자가 예상 못 한 사건을 가로막고 알리는 것 (청산됨, 실거래 파일 다운로드, 레버리지 청산 경고) | 상자 유지. radius 16 + 시맨틱 면 + 1.5px inset 링. `.alert alert-risk`/`alert-warn` |

기준이 "심각한가"가 아니라 "끼어들었는가"인 게 핵심이다. 심각해도 흐름 안에 있으면 notice다.

---

## 7. 깊이와 유리

평면이 기본. 그림자는 **떠 있는 것**(모달·토스트·메뉴·선택된 세그먼트)에만.

| 자리 | 유리 | 값 |
|---|---|---|
| 스티키 상단바 | ✅ | `rgba(11,14,17,.68)` + `saturate(180%) blur(20px)` |
| 하단 고정 시세 띠 | ✅ | 동일 |
| 모달 스크림 | ✅ | `rgba(0,0,0,.56)` + `blur(6px)` |
| 표 · 카드 · 배너 · 스탯 | ❌ | 아무것도 덮지 않음 |

**폴백 필수**: 반투명 배경색을 항상 먼저 깔아 `backdrop-filter` 미지원·비활성 환경에서도
글자가 읽히게 한다. 유리는 binance 명세가 금지하는 요소다 — 카드를 없애며 사라진 "명도 차 깊이"의
대체이므로, 이 세 자리 밖으로 확장하지 않는다.

**모션** — 120 / 200 / 320ms, `cubic-bezier(.22,.61,.36,1)`. 바운스 오버슈트 · parallax ·
skeleton shimmer 금지. `prefers-reduced-motion` 존중.

---

## 8. 카피

토스 어조 — **해요체 + 위임형 + 일상어**.

- 격식체(~합니다) · 단정형(~다) · 챗봇 톤("~해보세요!") · 마케팅 과장 금지.
- **버튼은 일어날 일을 직접 말한다** — `돌려보기`, `열기`, `송금하기`. `여기를 눌러주세요` 금지.
- 에러는 멈춰 세우지 않고 다음 화면으로 안내한다.
- 금액 `1,000원`(세 자리 구분 + 단위 붙여쓰기), 퍼센트 `12%`.
- **이모지**: 브랜드 마크(🦜)와 시각 자산 위치(아바타 슬롯·빈 상태)는 허용, 본문 문장 안
  인라인은 지양. 기존 카피의 이모지는 유지하되 새 카피에 늘리지 않는다.

---

## 9. 구현 매핑

### 토큰 위치
- **`frontend/src/index.css`** — 모든 색이 `--c-*` CSS 변수(R G B 트리플릿). `.dark`가 같은
  변수를 재정의한다. 컴포넌트는 `text-slate-700` 같은 유틸리티를 그대로 쓰고 테마는 자동.
- **`frontend/tailwind.config.js`** — 변수→Tailwind 매핑. `brand`/`brand-hover`/`brand-ink`/
  `brand-line` + `fontFamily.sans`(Pretendard) / `fontFamily.mono`(JetBrains Mono).

### slate 스텝 ↔ 역할 (핵심 — 외우는 표)

| Tailwind | 다크 | 라이트 | 역할 |
|---|---|---|---|
| `slate-50` | `#0b0e11` | `#f6f8fa` | 캔버스 / 페이지 바닥 |
| `slate-100` | `#1e2329` | `#eef1f4` | 면 |
| `slate-200` | `#2b3139` | `#dee3e7` | 괘선 |
| `slate-300` | `#3a424e` | `#c5cbd2` | 강한 테두리 |
| `slate-400` | `#9aa4b2` | `#6a7480` | **장식 전용** |
| `slate-500` | `#c9cfd8` | `#4b5765` | 흐린 글자 (기존 코드 최다 사용처 — 읽히는 하한으로 상향) |
| `slate-600` | `#b7bec9` | `#3d4855` | 본문 |
| `slate-700` | `#c9cfd8` | `#2d3a48` | 작은 글씨 |
| `slate-800` | `#dde1e7` | `#202b38` | 강조-1 |
| `slate-900` | `#eaecef` | `#141f2c` | 강조 |

다크에서 500·700이 600보다 밝은 것은 버그가 아니라 §1-1 규칙이다.

### 컴포넌트 클래스 (`index.css` `@layer components`)

§6 규격은 **클래스로 못 박혀 있다.** 예전에는 화면마다 같은 입력·버튼을 조금씩 다르게 다시
적어서(높이 40/44/48, radius 8/12, 포커스 링 indigo/brand) 규격이 흘렀다. 새 화면은 아래만 쓴다.

| 클래스 | 규격 |
|---|---|
| `.t-display` `.t-metric` `.t-h2` … `.t-caption` | §3-2 조판 램프 9단계 (§3-3) |
| `.measure` | 읽는 글의 한 줄 길이 상한 68ch |
| `.field` / `.field-sm` / `.field-err` | 48px·radius 12·면 배경 / 40px 축소판(밀집 폼) / 하락색 테두리 |
| `.btn` + `.btn-xl\|l\|m\|s` + `.btn-primary\|secondary\|ghost\|danger` | 크기 4단 × 채움 4종. 눌림은 `brightness(.86)` |
| `.badge` + `.badge-ai\|risk\|mine\|flat` | 22px·radius 6·12/700 |
| `.chip` / `.chip-on` | 34px pill, active는 브랜드 채움 |
| `.seg` / `.seg-item` / `.seg-item-on` | 세그먼트. 선택 분절은 라이트만 그림자 |
| `.notice` / `-risk` / `-warn` / `-good` | 왼쪽 규칙 2px + 들여쓰기 |
| `.alert` / `-risk` / `-warn` | 끼어드는 경고만. 상자 유지 |
| `.dialog` / `.scrim` / `.form-surface` | 모달 radius 20 + 그림자 / 검정 56% + blur 6 / 인라인 폼용 무그림자 표면 |
| `.chip-sm` | 조밀한 자리용 축소 칩 (수정자라 `.chip` 뒤에 정의) |
| `.table-row` `.row-label` `.row-value` `.stat-label` `.stat-value` | 상자 없는 표·스탯 |
| `ConfirmDialog` (`components/`) | `window.confirm` 대체. `.dialog` 안에 제목·설명·(경고 `.alert-warn`)·**세로 CTA 2개**(48px, 주 동작 위). ESC·스크림 클릭으로 닫힘 |
| `ChatBox` (`components/`) | 리더보드 채팅. 하단 '오늘의 경주마' 바 **위 14px에 떠 있는 48px 알약**(브랜드 노랑 바탕 + 깃털 28px + 잉크 'Chat' 15px (§1-4 예외 — 아래), surface·slate 링 + 그림자, 새 소식이면 빨간 링 + `N new` 배지) → 위로 열리는 380×600 시트(상단바·하단 바·버튼을 뺀 높이 안). 메신저 말풍선. 보드는 아래 88px 여지를 둬 마지막 행이 버튼 위로 올라온다 | 스티커: 작성줄의 껄무새 버튼 → 6칸 트레이, 누르면 즉시 전송(`[sticker:id]` 토큰), 말풍선 없이 92px 그림
| `RunResultScreen` (`components/`) | 실행이 끝나면 에이전트 채팅 자리를 **통째로** 차지. 아이브로 → 헤드라인 → 총 실현손익(큰 숫자, 상승 초록·하락 빨강) → 실행 시간·종목·남은 포지션 행. 상자 없음 |

화면 골격(`components/Page.jsx`)도 같은 이유로 공용이다 — `PageHeader` `SectionTitle`
`EmptyState` `EmptyRow` `Loading` `ErrorNote`. **페이지 머리는 `PageHeader` 하나로 통일한다**: 아이브로(11/750/자간 .08em) → 제목(clamp 30~44px/800/−.03em) → meta(15/600, 수치는 `.num` 본문색) → 설명(16~19/500, 40ch) → note(13/600 고지). 우측 액션은 제목 밑선. 제목이 없는 화면(내 에이전트)과 로그인 셸·글 제목(게시글)·내 활동(사용자 이름이 곧 제목, §6 profile)은 예외.
실행기 설치(`/runner/install`)도 2026-09-09 부터 이 컴포넌트를 쓴다 — 전용 `t-h2` 제목을 걷어냈다.
실행기 제목 옆에는 `ggparrot-nav-agent.svg` 원본을 배치한다(96px, 모바일 72px). 이미지 안의 빈 여백을 상쇄해 제목과 눈높이를 맞추고, 설치 애니메이션 위에 별도 장식 영역을 만들지 않는다.
코인동향은 아이브로를 쓰지 않는다(제목이 곧 그날의 머리). 오른쪽 열의 섹션 제목은 페이지 제목의 세로 중심에 맞춘다. 페이지마다 제목 줄과 "불러오는 중…"을 따로
짜면 화면을 옮길 때마다 눈이 다시 적응해야 한다.

`.dialog`는 §6이 말하는 "면 배경"이 아니라 **surface**를 쓴다 — 면(`slate-100`)으로 깔면 그 안에
놓인 `.field`(같은 면 색)가 배경에 묻힌다.

### 직접 만들기 워크벤치 (`/builder`, 2026-09-11)

- **페이지는 스크롤하지 않는다.** 본문(`.site-main.studio-main`)을 상단바·하단 경주마 띠를 뺀 높이에 고정하고(내 에이전트와 같은 틀),
  안에서 **조건(왼쪽 336px) · 차트(오른쪽 위) · 결과 독(오른쪽 아래, 44%·최소 280px)** 세 판이 각자 스크롤한다.
  조건을 만지면서 차트와 결과를 같은 화면에서 본다 — 이전의 `01 조건 정하기 / 02 결과 확인` 두 열(세로 2화면)을 대체한다.
- **제목 띠는 없다.** 페이지는 상단바 바로 아래에서 세 판으로 시작한다(가끔 생기는 알림 — 불러온 매크로·등록 이어가기·파일 등록 결과 — 만 위에 한 줄).
  모의 뱃지·단계 표시·⋯ 메뉴·사용법 안내(투어)는 두지 않는다(사용법은 상단바에 있다). `매크로 만들기` 제목은 sr-only.
- **매크로 파일 등록**은 조건 판 제목 `조건` 옆 28px 업로드 아이콘(로그인 전엔 로그인 링크). **공유 링크**는 매크로 등록 탭의 동작으로.
- **조건 판**: 머리 `조건` + `변경 뒤 자동 테스트` 체크. 본문은 공용 `Builder` 의 **촘촘한 판(`variant="dense"`, `components/Builder.css`)** —
  컨트롤 32px · 라벨 11px/600 흐린 글자 · 2열 격자 · 절은 아래 괘선 + 12px/700 제목. 종목은 **칩**(코인 로고 + 티커, × 로 빼고 점선 칸에 Enter·쉼표로 더한다),
  포지션은 **segmented(롱 | 숏)**, 숫자 칸의 단위(`%` · `USDT` · `시간`)는 라벨이 아니라 **칸 안 오른쪽 접미사**, 고급 위험 관리·비용은 접이식(제목 오른쪽에 `미사용` / `수수료 0.1% · 슬리피지 0.05%` 같은 상태 한마디).
  도움말 문장은 두지 않고 라벨 옆 ⓘ 하나로 — 종목(`symbols`: 여러 종목이면 자금 균등 분배), 포지션(`position`: 롱·숏 한 설명). 필드 목록·검증은 기본 판과 같다.
  바닥에 백테스트 버튼이 항상 붙어 있다 — 결과가 없거나 조건이 바뀌면 노랑(`이 조건으로 백테스트` / `바뀐 조건으로 다시 테스트`), 결과가 최신이면 2차(`결과 최신 · 바꾸면 다시 계산해요`).
  검증 오류·API 오류는 버튼 위 한 줄. `Ctrl+Enter` 안내는 버튼 아래 캡션.
- **차트 판**: 따로 머리를 두지 않는다 — 공용 `CandleChart` 의 **작업대 판(`variant="studio"`)** 이 곧 머리다. 도구줄 한 줄(44px): `종목 · 현재가 · 등락 · LIVE` | 봉 간격 **segmented**(select 대신) · 확대 −/+ · `최신` | 오른쪽 끝 범례.
  시세 읽기(OHLC)는 그림 왼쪽 위에 겹치고, 그림은 **판의 남는 높이를 재서 픽셀 그대로 채운다**(viewBox = px, 글자 11px, 오른쪽에 가격 눈금 4개). 아래 한 줄은 `시작 시각 · 진행 중/과거 구간 · 끝 시각`.
  봉 간격은 조건의 값과 같은 controlled 값, 오버레이는 지금 조건의 보조지표·익절/손절선. 종목이 여러 개면 380px 씩 세로로 쌓이고 판 안에서 스크롤한다.
- 세 판의 세로 비율은 차트 50% · 결과 독 50%(최소 320px). 백테스트·AI·최적화·페이퍼·등록 탭 모두 1400×1000 · 1440×900 에서 독 안 스크롤 0 이 기준.
- **작업 상태는 이 탭에 남는다**(`lib/studioSession.js`, sessionStorage): 조건·결과·탭·최적화 여부·공유 링크가 다른 화면(마이페이지 등)에 다녀와도, 새로고침해도 그대로다. 페이퍼 세션도 이어서 본다(`usePaperSession` 의 `resumeKey`). 공유 링크(`/s/:slug`)는 그 링크의 매크로가 우선이라 저장·복원하지 않는다.
- 조건 판의 ⓘ 말풍선(`InfoTooltip`)은 가장 가까운 스크롤 상자를 경계로 삼는다 — 오른쪽 열에서도 판 밖으로 나가지 않고(가로 스크롤 금지), 맨 위 필드에서는 아래로 편다.
- **결과 독**(`components/StudioDock.jsx`, 스타일 `.sd-*`): 머리는 **밑줄 탭**(44px, 13/700, 켜진 탭은 글자·밑줄 slate-900) `백테스트 · 껄무새 AI 해설 · 익·손절 최적화 · 페이퍼 트레이딩 · 매크로 등록` — **탭 순서가 곧 검증 순서**.
  탭 앞 7px 점이 상태(초록=완료 · 노랑 깜박임=진행 중 · 호박=조건이 바뀜). 결과가 없으면 백테스트 탭만 열린다.
  오른쪽 끝에는 **지금 단계의 다음 행동 하나**: 결과가 최신이면 `페이퍼 트레이딩 시작`(노랑), 페이퍼가 돌면 `매크로 등록으로 →`(2차)만, 조건이 바뀌면 캡션만.
  화면은 작업대 시안대로 **테두리 상자를 쓴다**(§1-3 의 예외 — 작업대 안은 구획이 많다). 데이터는 공용 부품과 같다.
  글자는 본문 13px · 캡션 12px · 수치 20px(`.num`) · 머리말 16px — 독은 화면 절반이라 작은 글자로 눌러 담지 않는다.
  독 안의 주의·경고는 **왼쪽 굵은 색 띠(`notice-*`)를 쓰지 않는다** — 얇은 테두리 상자(`.sd-box`)에 제목만 색을 준다(주의는 호박색 테두리 45% 섞음 + 굵은 제목).
  - 백테스트: **수치 띠 6칸** — 원래 `ResultView` 의 정보 세트 그대로, 관련 지표끼리 한 칸에(없던 지표는 만들지 않는다):
    `백테스트 수익률 · 기간` ⓘ(보조: `홀딩했다면 n% (+n%p)` — 괄호 안 차이만 등락색) · `최종 평가금액`(전체 금액 + 작은 단위 `USDT`, 보조 `≈ 원화`) · `MDD (최대낙폭)` ⓘ(보조 `최대 연속손절 n회` — 숫자만, 5회↑ 빨강) · `승률` ⓘ(보조 `총 매매 횟수 n`) · `샤프지수` ⓘ · `손익비 (PF)` ⓘ.
    캡션 13 · 값 22/700 `.num`(좁은 칸은 컨테이너 쿼리로 20 → 18) · 보조 13. **`.num`(고정폭)은 숫자에만** — 한글까지 넣으면 대체 글꼴로 빠져 글자가 깨져 보인다.
    여섯 칸은 **같은 폭**(좁은 칸은 값 글자만 줄고, 아주 좁으면 단위가 아래 줄로). → 아래 왼쪽 **자산곡선 상자**(공용 `EquityChart stretch` — 남는 높이를 채운다) · 오른쪽 **종목별 표** + 요약(레버리지 뱃지)·데이터 출처 캡션.
    오른쪽 열에는 표만 둔다 — 사람 말 요약은 **제목 띠**로 올라가고(결과가 최신일 때 `종목 · 롱 · 전략 조건 · 손절 · 자금 · 봉 · 기간`), 종목 안내·데이터 출처 문구는 두지 않는다. 청산이 있으면 `.alert-risk` 유지.
- **조건 판 바닥의 안내·오류는 경고 상자 하나**(`.alert` 기반, 테스트 버튼 위)에 한 번에 하나만: 오류 > 범위 확인 실패(`다시 확인`) > 입력 오류 > 범위 초과(대안 버튼) > 조건 바뀜(`조건이 바뀌었어요 · 오른쪽 결과는 이전 조건의 것이에요`).
  독 머리나 결과 본문에는 이 안내를 두지 않는다. 버튼 아래 캡션은 `첫 결과 뒤부터 자동 테스트가 동작해요` 와 `Ctrl+Enter` — 봉 수 한도 숫자는 보여 주지 않는다.
- **봉 간격 × 테스트 기간**: 봉 간격은 **조건 판에서만** 고른다. 차트 도구줄의 봉 간격 알약은 표시 전용(`span`, 누를 수 없음)으로 조건의 값을 비추고, 서버 한도(`/api/backtest/limits`)를 넘는 간격은 조건 선택지에서 `· 불가`로 비활성, 차트에서는 흐리게. 기간을 늘려 지금 간격이 불가능해지면 가능한 다음 간격으로 자동으로 옮긴다.
  - AI 해설: 얼굴 44px + `껄무새 AI 해설` + 머리말 15/700 + 요점 목록 + `이 매크로를 쓴다면`. 규칙 기반 문장은 보이지 않고(제품 결정) `AI 로 분석하기`(2차) 를 누르면 채워진다.
  - 최적화: 왼쪽 **열 지도**(CSS grid, 셀 32px 한 줄 — 학습 수익률만, 검증 값은 title/aria 로; 색은 본전 회색에 초록·빨강을 |수익률| 비례로 섞음; ★ 최적=브랜드 테두리 · 지금 설정=얇은 회색 테두리, 누르면 적용).
    오른쪽 상자 셋: `★ 최적`(익절 n% · 손절 m%) · `검증 구간`(검증 수익률 · 지금 설정) · 주의(`verdict` 판정 또는 과최적화 한 문장) → `익절 n% · 손절 m% 적용`(2차). `다시 최적화`는 머리 줄 오른쪽 고스트. A 전략만.
  - 페이퍼 트레이딩: 왼쪽 방식 segmented(실시간 | 데모 리플레이) + 잠금 안내(`.sd-lock` 면) → 돌기 시작하면 **상태 상자 2×2**(상태 · 현재 수익률 · 평가금액 · 청산) + 시작 시점 설정 한 줄 + `중지`/`재시작`/`마이페이지`. 오른쪽 **매매 로그 상자**(시각 · 구분 · 체결가·수량 · 누적수익, 최신이 위, 260px 안에서 스크롤).
  - 매크로 등록: **내가 만든 매크로가 트레이딩 카드 한 장(산출물)**으로 나온다(`.sd-card`, 폭 대부분). 카드 안은 절취선(파선)으로 두 면 —
    **왼쪽 "무엇인지"**: 종목 로고(`CoinIcon` 44px) + 티커 24px/800 + 그 아래 매매 방식(`RULE_TYPES` 라벨, 12px 흐림) · 오른쪽 위 포지션 배지(롱 초록 틴트 · 숏 빨강 틴트)와 시장 태그(`현물 · 1배` / `선물 · 격리 n배`) →
    조건 문장(서버 `human_summary` 의 핵심, 리더보드 전략 칸과 같은 값)은 `lib/strategyText.js` 로 조판한다: ` · ` 로 나뉜 구(句)는 세로 괘선(12px 여백)으로 가르고, **사용자가 정한 숫자**(A~K 전 유형 — 익절/손절 %, 지정가, DCA 간격·금액, 그리드 범위·격자 수, 트레일링 %, RSI 기간·임계값, 볼린저 기간·σ, 마틴게일 주문·횟수, k, MA 기간, K 의 하락·부분매도·숏 익절/손절)는 고정폭 1.1em/800 **노랑**(`amber-700`: 라이트 짙은 호박, 다크 밝은 노랑), 단어는 600. 등락색은 쓰지 않는다. **한 줄 고정 — 줄바꿈도 말줄임도 없다**: 폭이 모자라면 19px(좁은 카드 17px)에서 들어갈 때까지 글자 크기를 줄인다(`useFitLine`: 폭 변화와 웹폰트 로드 뒤에 다시 잰다; 전문은 title). 끝의 `n배 레버리지(격리)` 구는 머리의 시장 태그와 겹치므로 뺀다. 영문에 붙은 숫자(`RSI14`)는 떼지 않는다 → 사양표 2열(라벨 11px 흐림 / 값 13px 600): `종목 n개`(여러 종목일 때) · `봉 간격` · `자금`(초기 자금 + 투입 비율, C 는 회당 자금·간격) · `위험 관리`(손절 · 일일 최대손실 · 최대 보유 · 재진입 금지 중 켜진 것만, 없으면 `손절 없음`) · `비용`(수수료 · 슬리피지 · 선물이면 펀딩). 매크로 파일에 없는 값은 만들지 않는다.
    **오른쪽 "테스트에서 낸 것"**(38%, 최소 280px): `백테스트 수익률 · 기간` 라벨 + 26px 등락색 수치 → 자산곡선 미리보기(선 하나, 파선이 본전, 남는 높이를 채운다) → `MDD · 승률 · 매매 n회` 세 칸 → `데이터 · 출처`(cache/binance/binance-futures/synthetic 를 사람 말로). 오른쪽 위 `GGPARROT MACRO` 눈썹.
    카드는 브랜드 틴트 그라데이션(왼쪽 위 16% → 면) + 18px 모서리 + 그림자. 카드 폭이 620px 이하(1300~1540px 창)면 결과 면이 카드 아래 한 줄 띠(수익률 · 곡선 · 세 칸)로 내려온다(컨테이너 쿼리 — 카드 자신이 컨테이너라 안쪽 `.sd-card-in` 이 두 면을 나눈다).
    오른쪽 **300px 동작 목록**(`.sd-act`)은 **카드와 높이가 같다**(grid stretch, 행 넷이 `flex: 1` 로 높이를 똑같이 나눔): `리더보드 등록`(노랑, 결과가 최신일 때만) · `빠른 실행` · `매크로 파일 내려받기` · `공유 링크 보기`(링크가 없거나 조건이 바뀌었으면 저장해서 만든 뒤 다이얼로그) — 각 행은 아이콘 칩 30px · 이름 14px/700 · 한 줄 설명 12px 흐림 · 갈매기.
    그 아래 **실행기 실거래 안내**는 원래의 호박색 `alert-warn` 상자 안을 **제목 줄 + 두 열**로 정리한다(`.sd-runner-*`): 제목 `실거래 실행법` 15px/800 → 괘선 → 왼쪽 `진행 방법` 번호 원(20px, amber-700) 3단계(굵은 제목 + 설명): ① 테스트넷으로 먼저(`빠른 실행` 마법사 — 내 매크로 저장 → 테스트넷 키 → 실행기 받기 → 웹이 실행기를 열어 줌 → 실행기에서 `매크로 시작`) ② 실거래로 바꾸기(.ggm.json 을 실행기 ①에서 열고 ② 실거래(메인넷) 체크 ③ 실거래 API 키 ④ 회원 키 — 회원 키는 `/runner/install` 링크) ③ 지켜보기·멈추기(내 에이전트, 매크로만 종료 / 청산 후 종료). 오른쪽 열의 제목이 곧 경고문 `주의: 실행기는 실제로 주문을 실행해요 (기본값: 바이낸스 테스트넷 = 가짜 자금)`(13px/700), 그 아래 글머리 5개는 원문 표현 그대로: `롱·1배 매크로라 현물(spot)로` / `숏·레버리지 매크로라 USDT-M 선물로` · `익절·손절·일일 최대손실·최대 보유시간·재진입 금지가 함께 적용` · `실거래(메인넷) 체크를 켜야 움직임(경고 확인 단계 있음) · 빠른 실행은 테스트넷만` · `API 키는 실행기 로컬에서만 · 출금 기능 없음` · `브라우저는 닫아도 되나 PC·실행기를 끄면 멈춤`. 원문의 정확한 용어(spot · 기본값 · 재진입 금지 · 메인넷 체크)를 줄여 쓰지 않는다. 바닥에 괘선 + 한 줄 설명(Windows PC 에서 주문을 처리하는 프로그램 · 설치 없이 실행 · 웹은 주문 안 냄 · 상태/종료는 **내 에이전트** — 굵은 글자일 뿐 링크가 아니다). 예전 흐름(마이페이지 회원 키 · 마이페이지에서 종료)은 쓰지 않는다. 문장은 짧게, 한 항목에 한 가지.
    근거 줄·상자 여럿은 두지 않는다. ≤1300px 는 카드와 목록을 세로로 쌓고 행은 제 높이로 돌아가며, 안내 상자의 두 열도 한 열이 된다.
- **노랑은 한 번에 하나**(§1-4): 조건 단계 = 백테스트 버튼, 검증 단계 = 페이퍼 시작, 등록 단계 = 리더보드 등록. 나머지는 2차·고스트.
- ≤1099px: 한 열로 쌓는다(차트 → 조건 → 결과), 페이지가 스크롤한다. 단계 표시·조건 요약은 숨긴다.
- 스타일은 `pages/Studio.css` 한 파일. 부품(`Builder`·`ResultView`·`OptimizePanel`·`PaperPanel`·`CandleChart`)의 규격은 손대지 않았다.

### 통합 시작 화면과 모달형 도움말 (`/`)

- `/`의 첫 화면은 “비트코인 매크로가 하고 싶다면 껄무새에서 시작한다”는 목적을 크게 설명하는
  히어로다. 오른쪽에는 노란 primary `매크로 바로 만들기`, 첫 사용자를 위한 `처음부터 안내`,
  모든 조건을 직접 설계하려는 사용자를 위한 `매크로 직접 만들기`의 큰 진입 행동 세 개를 둔다. `매크로 바로 만들기`를
  누른 뒤에만 같은 `/`의 `?run=1&step=1` 상태에서 실제 **매크로 선택 → API 키 준비 → 실행기 준비 →
  계정 연결 → 실행 확인** 흐름으로 전환한다. 가짜 가입자 수·수익률·회사 로고, 장식용 그라디언트,
  떠다니는 코인, bento 카드 그리드는 쓰지 않는다.
- 좌측 사이드바는 **실행 / 둘러보기** 두 그룹으로 나눈다. 실행에는 `매크로 바로 만들기`와
  `직접 만들기`만 두고, 둘러보기에는 `리더보드 · 코인동향 · 게시판`을 둔다. 빠른 실행과 사용
  가이드를 별도 페이지 링크로 중복하지 않는다. 상단바에는 `사용 방법` 외의 페이지 링크를 두지 않고
  시장 참고 지표와 계정 도구만 둔다. 모바일도 같은 다섯 메뉴를 포커스가 갇히는 좌측 드로어로 제공한다.
- 사이드바의 페이지 표식은 선형 SVG를 섞지 않고 `public/brand/navigation/`의 투명 껄무새 캐릭터
  자산으로 통일한다. 36px 슬롯 안에서 36px 정사각 이미지로 표시하고, 텍스트 라벨이 이름을 제공하므로
  이미지는 장식 이미지(`alt=""`)로 처리한다. 캐릭터 뒤에 별도의 원형·사각형 아이콘 타일을 겹치지 않는다.
- `사용 방법`은 히어로나 실행 화면의 작은 텍스트로 숨기지 않고, 모든 제품 화면의 헤더에서
  44px hit area와 고대비 라벨을 가진 **ghost control**로 제공한다. 평상시에는 면과 외곽선을 두지 않고
  hover·focus·문서 열림 상태에서만 인접한 계정 컨트롤과 같은 면·선을 쓴다. 노란 채움은 히어로의 `매크로 바로 만들기` 또는 각 실행
  단계의 primary CTA 하나에만 사용한다. `?guide=1`은 아홉 단계 체험 가이드, `?help=start`는 검색
  가능한 문서형 가이드를 같은 화면 위의 모달로 연다.
  `/runner?step=*`와 `/guide`는 위치 상태와 쿼리를 보존해 각각 통합 시작 단계와 문서 모달로
  `replace` 이동하는 호환 주소로만 유지한다.
- 가이드는 자동으로 띄우지 않는다. 미완료 사용자의 마지막 장면과 매크로 초안만 보관하되,
  백테스트·등록 성공을 저장값으로 꾸며내지 않고 필요하면 실제 검증부터 다시 실행한다. `tour`는 현재
  장면, `resume`은 로그인 뒤 등록 복구만 나타내며, 현재 장면 변경은 URL을 replace한다.
- 가이드 외곽은 헤더 아래를 채우는 어두운 scrim + blur이고, 사이드바보다 위·상단바보다 아래에 둔다.
  따라서 사이드바 도입 뒤에도 왼쪽이 가려지지 않고 전체 뷰포트 폭을 쓴다. 실제 내용 본체는 불투명 `surface`다.
  가이드가 열려도 좌측 상단의 껄무새 브랜드 링크는 활성 상태를 유지해 홈으로 돌아갈 수 있게 하고,
  그 외 헤더 도구와 사이드바 메뉴만 `inert` 처리한다.
  데스크톱 본체는 화면에서 12–14px만 물러난 최대 1560×940px, 모바일은 헤더 아래 edge-to-edge다.
  **진행률 → 현재 장면 → 하단 이동**의 3행 골격과 고정 하단 버튼을 유지한다. 데스크톱의 긴 결과는
  오른쪽 결과 영역만, 작은 화면은 가운데 장면 전체만 내부 스크롤한다. 진행률의 챕터 제목은
  해당 챕터 첫 화면으로 이동하는 버튼이며 현재 챕터는 별도 박스나 장식선 없이 글자 굵기와 명도로만
  구분하고 `aria-current="step"`을 제공한다. 모달 헤더의 `껄무새 가이드라인` 타이틀은 가이드 첫
  화면으로 돌아가는 버튼이다.
- 모달은 `dialog` 이름, ESC, 44px 닫기, 포커스 트랩, body scroll lock, 닫은 뒤 opener 포커스 복원을
  제공한다. 내부 등록 모달이 열려 있을 때는 상위 ESC/Tab 처리를 멈춰 두 모달이 함께 닫히지 않게 한다.
- 큰 흐름은 **매크로 빌드 → 종목 검색 → 조건 설정 → 백테스트 → 페이퍼 →
  리더보드 등록 → 커뮤니티 → 코인동향 → 가이드**의 아홉 챕터다. 종목을 검색한 뒤
  별도의 차트 확인 단계를 두지 않고, 조건 설정 중 왼쪽에 같은 실시간 차트를 계속 유지한다.
  전략·전략별 핵심 조건·손실 제한·기간은 한 항목씩 나누며 차트에서 고른 봉 간격과 A–K 전략
  입력값을 실제 빌더로 그대로 넘긴다.
- 장면은 현재 화면만 DOM에 둔다. 전환은 200ms 이내의
  opacity·16px 이동, 오른쪽 화면의 40ms 지연, 진행률 320ms만 기본으로 쓰며
  `prefers-reduced-motion`에서는 제거한다.
- 장면 제목의 핵심 구절 하나는 노란 글자가 아니라 **slate 제목 + 노란 밑줄**로 표시한다. 라이트
  캔버스에서 노란 글자의 대비 부족을 피하고 CTA 채움과도 구분한다.
- 백테스트는 성공한 설정 스냅샷으로 실제 서버 계산을 실행하고 기존 결과 컴포넌트를 그대로 쓴다.
  이 결과와 같은 설정으로만 페이퍼 세션을 시작할 수 있으며, 페이퍼 시작 성공 뒤에만 등록으로 간다.
  체험 세션은 등록 전에 종료하고 실제 등록용 리더보드 집계 세션을 새로 시작한다.
- 등록 성공 뒤에는 이동 링크로 대신하지 않고 등록 API 응답을 즉시 실제 리더보드 행으로 표시한 뒤,
  전체 목록을 백그라운드에서 다시 받아 실제 순위·수익률·초기화 시간을 같은 장면에 동기화한다.
  등록 장면에 머무는 동안 목록은 5초마다, 초기화 카운트다운은 1초마다 갱신한다. 전체 목록에 새
  항목이 아직 없으면 임의의 순위를 만들지 않고 `—`와 반영 대기 상태를 표시한다.
- 커뮤니티와 코인동향은 자리표시자 도식이 아니라 **실제 제품 화면 크기와 정보 밀도를 유지한
  고충실도 목업**으로 보여준다. 커뮤니티에는 리더보드를 반복하지 않고 채팅과 게시판을 각각
  전체 폭으로 세로 배치한다. 예시 대화·게시글·기사·등락률은 `화면 목업 · 예시 데이터`라고 명확히
  고지하며 실제 API나 등록 동작을 호출하지 않는다. 입력과 버튼 모양은 프레젠테이션 전용 DOM으로
  만들어 작동하는 컨트롤처럼 포커스되지 않게 한다.
- 가이드 장면은 별도 축약본을 만들지 않고 실제 `Guide`의 첫 문서(`껄무새가 뭐예요?`)를 lazy로
  임베드한다. 검색과 목차는 그대로 동작하며 상단 내비게이션의 `둘러보기`에 2px 노란 밑줄과
  `가이드` 문구를 표시해 **둘러보기 → 사용 가이드**라는 실제 진입 위치를 알려준다.
- 후기 showcase 장면은 최대 1480px 폭에서 왼쪽 설명 260–300px, 오른쪽 화면 나머지 폭을 사용한다.
  오른쪽만 내부 스크롤하며 축소 변환(`scale`)은 사용하지 않는다.
- 한 장면의 강조 행동은 노란색 버튼 하나로 제한한다. 언제든 전체 빌더로 빠질 수 있는 보조 링크와
  키보드 포커스 이동, 단위·도움말·오류 연결을 함께 제공한다.

### 테마 양립 — 단계 고르기 (실수 잦은 곳)

팔레트가 테마별로 뒤집히므로 **양쪽에서 다 읽히는 단계만** 글자에 쓸 수 있다. 아래는 실제로
다크에서 안 보였던 것들이다. 링크·포인트·경고 글자를 새로 쓸 때 이 표를 먼저 본다.

| 쓰면 안 되는 것 | 왜 | 대신 |
|---|---|---|
| `text-indigo-600` · `text-indigo-500` | 다크에서 어두운 남색 → 캔버스에 묻힘 | **`text-indigo-800`** (라이트 진한 남색 / 다크 밝은 남색) |
| `text-blue-600` | 동일 | **`text-blue-800`** |
| `text-amber-600` | 다크에서 어두운 주황 | **`text-amber-700`** (라이트 갈색 / 다크 노랑) |
| `bg-slate-800` + `text-white` | 다크에서 slate-800이 밝아져 흰 글자가 사라짐 | **`bg-slate-900 text-slate-50`** (두 테마에서 서로 뒤집히는 짝) |
| `bg-red-600` + `text-white` | 다크 red-600은 밝은 분홍 → 3.3:1 | **`bg-danger`** (채움 전용 토큰) |
| `bg-green-600` 채움으로 등락 표시 | §2-1 위반 + 흰 글자 대비 미달 | 글자 색으로만 |
| `green-200/800`, `red-200` | **Tailwind 스케일에 아예 없다** — 클래스가 생성되지 않아 조용히 무시됨 | `green-300/700`, `red-300` |

정의된 단계는 `tailwind.config.js`의 `scale(...)` 배열이 전부다: green은 `50·300·600·700`,
red는 `50·100·300~800`, sky/cyan은 `50·100·(200)·700·800`뿐이다.

### 유틸리티
- **`.num`** — JetBrains Mono + `tabular-nums` + 자간 −0.01em. 모든 수치 표면(수익률·순위·
  금액·시세·카운트다운·시각·심볼)에 붙인다.
- 차트는 Tailwind를 못 쓰므로 `--chart-grid/axis/up/down` 변수를 직접 읽는다.

### 상태 축약
```
hover   : 채움 한 단계 위 (brand는 brand-hover)
active  : brightness(.82~.86) 또는 검정 26% overlay
focus   : :focus-visible → brand-line 1.5~3px (outline-none 단독 금지)
disabled: 전체 opacity .30~.40
```

### 내 에이전트 작업공간

- 설명형 페이지 헤더나 상단 매크로 카드를 반복하지 않는다. 헤더 바로 아래 왼쪽에서 큰 차트가
  시작하고, 차트 하단의 한 줄 실행 바에서 내 매크로 선택·요약·상태·연결 설정·실행 제어를 끝낸다.
- 화면은 헤더 아래 남은 높이를 정확히 채우며 페이지 자체를 스크롤하지 않는다. 데스크톱은 왼쪽
  차트 작업면과 오른쪽 전체 높이 에이전트 기록을 나란히 두고, 900px 이하에서는 `차트 / 에이전트`
  탭으로 같은 높이 안에서 전환한다.
- 실시간 차트는 실행 중일 때만 보이는 세션 부속물이 아니다. 선택한 매크로가 멈춰 있어도 항상
  표시하며, 실행 중일 때만 평단·포지션·손익을 추가한다. 차트 영역에는 내부 스크롤을 만들지 않고
  반응형 자연 높이로 현재가·조작부·OHLC·보조지표를 한 번에 보인다. 현재가는 종목명보다 크게 두고,
  OHLC는 고정된 열과 동일한 간격으로 정렬한다. 사용법·면책·지표 해설을 차트 아래 반복하지 않는다.
  포인터가 차트 위에 있을 때
  일반 휠은 확대·축소에만 쓰고, 브라우저 확대 단축키는 가로채지 않는다. CandleChart 자체가 보여주는
  종목·가격 정보 외에 `BTCUSDT 차트 / 공개 시세 / 자동 갱신` 같은 중복 외부 헤더는 두지 않는다.
- 에이전트 출력은 오래된 것부터 최신 순으로 쌓이는 왼쪽 발화 기록이다. 각 발화는 에이전트 마크,
  모듈·구독 등급·시각, 핵심 관측·근거·출처 순으로 읽는다. 두꺼운 색 띠, 반복 카드 테두리, 점 세 개
  타이핑 연출은 쓰지 않는다. 사용자가 이전 내용을 읽는 동안에는 강제로 최신 위치로 이동시키지 않고
  새 관측 개수 버튼으로 복귀를 제안한다.
- 사용자 입력을 처리하는 API가 생기기 전에는 가짜 채팅 입력창을 만들지 않는다. 생동감은 실제 데이터
  확인 중 상태, 상태점, 새 관측의 자연스러운 추가로만 표현한다.
- 새 에이전트 기능은 화면 레이아웃을 복제하지 않고 모듈 레지스트리에 `key`, 표시 이름,
  entitlement와 `minimumPlan`을 추가한다. 구독은 `free / plus / pro` 세 단계이며 실행 상태·종목 뉴스는
  Free, 위험 감시는 Plus, 전략 조건·대장주 신호는 Pro를 최소 등급으로 둔다. Free 발화에는 배지를
  반복하지 않고, Plus·Pro 발화에는 각각 `PLUS 전용`, `PRO 전용` 텍스트 배지를 붙인다. 배지만으로
  구분하지 않고 Plus는 인디고 계열, Pro는 브랜드 옐로 계열의 절제된 외곽선·배경 톤을 말풍선 전체에
  적용한다. 각 모듈은 `사용 중 / 구독 필요 / 준비 중 / 오류` 상태를 가진다.
- 공개 캔들에서 다시 계산한 조건은 `전략 조건 표시`, 실행기가 보고한 체결만 `주문·체결`로 부른다.
  뉴스에는 매체·원문 링크를 남기고, 확인할 데이터가 없는 신호는 생성하지 않는다.
- 구독 기능은 화면 잠금만으로 보호하지 않는다. 에이전트 이벤트 조회·생성 API에서 같은 entitlement를
  검사한 뒤 허용된 데이터만 내려보낸다.

---

## 10. 명세를 벗어난 지점

의도적으로 어긴 것들. 되돌릴 때 참고.

| 벗어난 것 | 원 명세 | 이유 |
|---|---|---|
| 카드를 쓰지 않음 | binance `markets-table-card` | 회색 사각형이 화면을 답답하게 만듦. spacex 방식으로 대체 |
| 보조 텍스트를 밝힘/진하게 | binance `muted` `#707a8a` | 4.1:1로 AA 미달 |
| 작은 글씨용 `#c9cfd8` 신설 | binance 팔레트에 없음 | `#eaecef`↔`#929aa5` 사이가 비어 "읽어야 하는 작은 글씨" 단계가 없었음 |
| `#929aa5` 퇴출 | binance `muted-strong` | 6.8:1 — AA 통과지만 halation으로 실독성 미달 |
| 유리 사용 (3자리 한정) | binance "글래스모피즘 금지" | 카드 제거로 사라진 명도 차 깊이의 대체 |
| `small` 14/500, `caption` 13/600 | toss 13/400, 12/500 | 다크에서 안 읽힘. 비율 유지, 바닥만 상향 |
| 다크 팔레트 전반 | toss는 라이트 전용 | 토스 명세 스스로 다크 alias 미공개 명시 |
| 하락 `#c81e33` (라이트) | toss `red-500` `#f03848` | 흰 배경 3.9:1 미달 → 상승과 5.7:1로 명도 매칭 |
| 이모지 일부 유지 | toss "인라인 이모지 금지" | 🦜 껄무새가 브랜드 자체. 아바타는 글자로 전환 완료 |
| 아바타 = 이름 첫 글자 | (양쪽 명세에 규정 없음) | 이모지가 36px 원에서 흐릿 → 식별성·정책 동시 해결 |
| 상단바 CTA를 secondary로 | 토스 "가입은 주요 행동" | 상단바는 모든 화면에 얹히는 크롬이라 노란 채움을 두면 본문의 primary와 **항상** 겹친다(§1-4). 노랑은 화면 본문이 가져간다 |
| 리더보드 언락 버튼 secondary | 결제성 행동 = primary 관행 | 행마다 하나씩이라 primary로 두면 화면에 노란 버튼이 열 개가 된다. 페이지 primary는 상단 '등록' 하나 |
| 참고 지표 띠에서 색 면 제거 | (자체 결정 되돌림) | 김프·공포탐욕·한강 셋이 각각 slate/sky/cyan 면을 깔아 상단이 색 블록이 됐다. 정체성은 라벨 글자색만 남기고 구분은 괘선 한 줄로 |
| 도구 패널(차트·최적화·페이퍼·채팅) 상자 제거 | §11의 보류 항목 | 재검토 결과 §1-3을 그대로 적용. 셋 다 표·목록·스탯이라 괘선으로 충분했다 |
| 히트맵 셀 보조 라벨 11px | §1-7 (13px 하한) | 격자 셀 안 2줄 수치라 13px이면 칸이 깨진다. 데이터 시각화 내부 라벨에 한정 |

## 11. 알려진 미적용 / 추후 판단

전면 적용(2026-08-05)으로 아래 항목이 정리됐다. 남은 것만 적는다.

- **가이드 화면 테마 고정** 여부 — 현재는 전역 토글을 따른다. "배우는 화면은 라이트 고정"(binance
  용도별 모드 규칙) 안은 여전히 보류.
- 한국 관습(빨강=상승) 문제는 팔레트와 별개 결정으로 미해결 — 현재 글로벌 관습(초록=상승) 유지.
- 상자를 **의도적으로 남긴 곳**은 셋뿐이다: 로그인·비밀번호 폼, 게시판 글쓰기 폼, 등록 모달.
  전부 §1-3이 명시한 "폼·모달" 예외이고, 화면 전체가 하나의 입력 흐름이라는 공통점이 있다.
  인라인 폼은 `.form-surface`로 그림자를 쓰지 않고, 실제로 떠 있는 등록 모달만 `.dialog` 그림자를 쓴다.
  (여기에 §6 `alert` 세 자리가 더해진다 — 청산·실거래 전환·레버리지 경고)

### 해결됨
- ~~상단바·하단 띠 유리 값~~ — `.glass`로 §7 규격 적용 (라이트는 흰색 72% 변형).
- ~~도구 패널 상자~~ — CandleChart·OptimizePanel·PaperPanel·ChatBox 전부 괘선 구획으로 전환.
- ~~화면마다 다시 적던 입력·버튼 규격~~ — §9 컴포넌트 클래스로 통일.
- ~~다크에서 안 읽히던 indigo/blue/amber 글자~~ — §9 "테마 양립" 표로 단계 확정.
