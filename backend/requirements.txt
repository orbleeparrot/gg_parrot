# Minimum versions verified on Python 3.14 (prebuilt wheels available).
# Pinned as >= so pip resolves 3.14-compatible wheels instead of building
# old pandas/numpy from source.
fastapi>=0.135.0
uvicorn[standard]>=0.44.0
pydantic>=2.12.0
sqlmodel>=0.0.39
pandas>=2.3.3
numpy>=2.4.0
httpx>=0.28.1
playwright==1.62.0
Pillow>=12.0.0
# 껄무새 AI 해설 / 데일리 챌린지 — 공식 Anthropic SDK (서버측 키만 사용).
anthropic>=0.40.0
# Loads a local backend/.env for dev secrets (e.g. GEMINI_API_KEY). Optional at
# runtime — main.py imports it defensively — but pinned so `pip install` provides it.
python-dotenv>=1.0.1
# Auth (JWT sessions) + Postgres driver for the durable store (Supabase).
# psycopg is only imported when DATABASE_URL points at Postgres; local dev/tests
# run on SQLite without it.
PyJWT>=2.9.0
# 게시판 이미지 업로드(multipart/form-data) 파싱 — FastAPI Form/File 의존.
python-multipart>=0.0.9
psycopg[binary]>=3.2.0
pytest>=8.3.0
